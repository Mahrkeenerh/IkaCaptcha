# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Training datasets — fetched via scripts/download_data.sh from the v1.0.0
# release, not committed directly (too large, and LFS has bandwidth quotas).
data/ikariam_pirate_captcha_dataset/
data/dataset_pseudo_v2/

# Synthetic training data — regenerate locally with `python scripts/generate_dataset.py`
dataset_synthetic/

# Demo output of `python generate_captcha.py`
generated_samples/

# Editor / OS
.vscode/
.idea/
.DS_Store
