# iKaptcha — Ikariam Captcha Solver

## Goal
Automate Ikariam pirate fortress captcha solving — either via browser extension or by upstreaming the model into ikabot/IkabotAPI.

## Key Discovery: Existing Solution
- **IkabotAPI** (https://github.com/Ikabot-Collective/IkabotAPI) — YOLOv8n object-detection model, 98.9% mAP on their internal test
- **ikabot** (https://github.com/Ikabot-Collective/ikabot) — Full Python bot with auto-piracy + captcha solving
- Their ONNX is shipped as `models/yolov8n-ikariam-pirates-mAP-0_989.onnx` (kept for baseline comparison)
- On the original 300-sample val: **78.7%** sequence accuracy / 81.2% on the corrected val

## Character Set — ONLY 28 chars (NOT 36)
Letters (24): A B C D E F G H J K L M N P Q R S T U V W X Y
Digits (4): 2 3 4 5 7
**Missing: 0 1 6 8 9 I O Z** — excluded by the game server because visually ambiguous

## Captcha Characteristics
- Source URL: `https://s53-cz.ikariam.gameforge.com/?action=Options&function=createCaptcha&rand=<random>`
- Server-generated PNG images, ~400x76px
- 4-8 chars from the 28-char set above (case-insensitive validation)
- Characters use fancy/3D fonts, random colors, rotation, spatial warping
- Noise: colored lines, outlined ellipses overlaid on top
- Background: solid pastel/light color (varies per image)
- Fetching a new captcha invalidates the previous one for that session

## Verify Endpoint
- `POST https://s53-cz.ikariam.gameforge.com/index.php`
- Params: `action=PiracyScreen&function=capture&captchaNeeded=1&captcha=<answer>&cityId=1033&position=17&buildingLevel=1&ajax=1`
- Auth: session cookie `ikariam=<value>`

## Our Model — CRNN (production, beats YOLO baseline)

| Model | Params | Original Val | Corrected Val |
|---|---|---|---|
| YOLOv8n (theirs) | ~3M | 78.7% | 81.2% |
| **Our CRNN (final epoch)** | **1.66M** | **95.0%** | **97.3%** |

Character accuracy: **99.5%** (ours) vs 96.5% (YOLO). All numbers are from the **final training epoch**, not cherry-picked.

### Architecture — `ikaptcha/model.py`
1.66M params CRNN:
- 4 ResBlocks with SE (Squeeze-Excitation) attention: 32→64→128→256 channels
- Last 2 blocks use MaxPool(2,1) to preserve horizontal resolution → T=64 timesteps
- AdaptiveAvgPool collapses height
- 1-layer BiLSTM (hidden=128), CTC head
- Input: 48×256 RGB, Output: T=64 × 29 classes

### Training — two phases, ~90 min total
1. **Pretrain** on ~65k synthetic (50 epochs, OneCycleLR max_lr=3e-4)
2. **Mixed** real + synthetic (40 epochs, OneCycleLR max_lr=2e-4, 3× real oversample via WeightedRandomSampler)

No warm restarts, no SWA — both were counterproductive with CTC. Weight decay 1e-2, dropout 0.2 (CNN) / 0.4 (FC). Gradient clip 5.0.

### Production Dataset (`data/dataset_pseudo_v2/`)
- 1,200 YOLO train (original, uncorrected)
- 99 hand-reviewed real samples
- 9,911 pseudo-labeled real samples at ≥0.99 confidence
- **Total: 11,210 real train + 298 corrected val**
- Plus ~65k synthetic generated by `scripts/generate_captcha.py`

### Production Model
- **`models/ikaptcha.pth`** — PyTorch CRNN, final epoch, 97.3% corrected val
- **`models/ikaptcha.onnx`** — ONNX export of the same weights, 100% prediction parity with PyTorch on all validation samples. Ship this. 6.4 MB. Loads cleanly via `onnxruntime` and `cv2.dnn` (≥4.8.0)

### Deployment
- `scripts/predict.py` — PyTorch inference (development/debug), loads `models/ikaptcha.pth`
- `scripts/predict_onnx.py` — ONNX inference (production), loads `models/ikaptcha.onnx`. Depends only on `onnxruntime` + `Pillow` + `numpy`. Use as the reference implementation when porting to other runtimes (`onnxruntime-web`, `cv2.dnn`, mobile, etc.)
- `scripts/export_onnx.py` — re-exports `models/ikaptcha.pth` to `models/ikaptcha.onnx` and verifies parity on both val sets

### Repo Layout
```
ikaptcha/                 importable package: CRNN, charset, decoders, transforms
scripts/                  entry-point scripts (predict, train, export, eval, generate, ...)
data/
  samples/                9 hand-labeled test images
  ikariam_pirate_captcha_dataset/   original 1,200/300 YOLO dataset
  dataset_pseudo_v2/      production 11,210/298 train/val
models/                   ikaptcha.onnx, ikaptcha.pth, yolov8n baseline
fonts/                    ~120 .ttf files for synthetic generation
dataset_synthetic/        65k synthetic captchas (gitignored, regen via scripts/generate_dataset.py)
```

See `FINDINGS.md` for full details.

## Labeled Samples (in `data/samples/`)
| File | Label |
|------|-------|
| test1.png | b45d5eee |
| test2.png | aqrckd3 |
| test3.png | 25t352j |
| test4.png | 5jjpe4b |
| test5.png | hp5xeqf |
| test6.png | xnwcprl |
| test7.png | arh3ml |
| test8.png | dtmbpw |
| test9.png | 7dxesdjv |

## Package Management
- Use `uv` for Python, not `pip`
- Use `uv venv` for virtual environments
- `uv pip install -e .` installs the `ikaptcha` package in editable mode
